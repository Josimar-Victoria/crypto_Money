import React from 'react'
import {Line} from 'chart.js'

export const VolumeChart = ({volumes: { dailyVolumes, weekVolumes, yearVolumes}}) => {
    return (
        <div>
            VolumeChart
        </div>
    )
}
